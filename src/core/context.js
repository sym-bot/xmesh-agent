'use strict';

// Context assembly — runtime doc §4.2. Pending mesh adapter integration.

async function assembleContext(/* { admittedCmb, role, mesh, limits } */) {
  throw new Error('assembleContext — not implemented (runtime doc §4.2)');
}

module.exports = { assembleContext };
